services:
  app:
    build: .
    container_name: m9m-app
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
    expose:
      - "3000"
    networks:
      - pokerllm_default

networks:
  pokerllm_default:
    external: true
